import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'playlist_all_artists_app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
